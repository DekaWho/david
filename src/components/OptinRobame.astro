---
import OptinBase from "./OptinBase.astro";

/* Placeholder del optin de Róbame, lead magnet retirado: su form se borró
   de MailerLite, así que no hay form-id que fijar. El cajetín se mantiene
   como pieza visual en la landing /robame — `cerrado` lo deja inerte
   (input y botón disabled, cartel "Acceso cerrado") y sin postear a
   ningún sitio. Para reabrir Róbame: crear form + automatización nuevos
   en MailerLite y convertir esto en un wrapper activo (fijar formId y
   quitar `cerrado`; ver OptinClaudeSkool como referencia). */
---

<OptinBase {...Astro.props} cerrado />
